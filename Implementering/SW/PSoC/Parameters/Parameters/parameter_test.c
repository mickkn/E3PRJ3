#pragma once

#include "parameters.h"



int main()
{
	//parameters myStruct;
	//parameters * Ptr_ = &myStruct;

	//parameters_init(Ptr_);

	//std::cout << Ptr_->active_ << " " << Ptr_->humidity_ << " " << Ptr_->temperature_ << std::endl;


	while (1);
}