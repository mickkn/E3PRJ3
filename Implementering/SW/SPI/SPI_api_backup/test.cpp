#include "SPI_api.h"
#include <fcntl.h>
#include <stdio.h>

int main(void)
{
	int testData = 1;

	//float tempTest = 100.1;
	//float humiTest = 48;

	SPI_api test;

	//test.activate(1);
	//test.deactivate(1);
	//test.config(1, tempTest, humiTest);
	//test.verify(1);
	//test.getLog(test_data, &testData, 1);

	return 0;
}
